<div class="banner">
  <h1>My list</h1>
</div>

<div class="card-white", p-3>
  <% @lists.each do |list| %>
    <%= link_to list_path(list), class:'text-decoration-none' do %>
      <div>
        <%= list.name %>
      </div>
    <% end %>
  <% end %>
</div>
